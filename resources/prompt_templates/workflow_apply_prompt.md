# 实施已确认的工作流优化

请仅实施用户在本次对话中明确确认采纳的优化条目。用户未明确确认的建议不得顺手实施。

项目组根目录：{{GROUP_ROOT}}

可能涉及的公共文件：

1. {{RULES_PATH}}
2. {{WORKFLOW_PATH}}
3. {{TEMPLATE_PATH}}
4. {{VALIDATOR_PATH}}

## 实施规则

1. 先复述将实施的已确认条目；确认内容不完整或有歧义时停止并询问。
2. 每条规则只写入最合适的一个公共文件，避免跨文件重复。
3. 保持已有项目和生产流程兼容，不覆盖任何历史项目数据或产品版本。
4. 修改 validate-tool.js 后运行验证，检查新增规则的正确报错与原有检查回归。
5. 修改 template.html 后验证制作流程仍可成立，并检查常用分辨率和关键交互。
6. 修改完成后检查四个公共文件之间是否存在冲突、重复或优先级不一致。
7. 输出实际修改文件、每项改动、验证结果和仍存在的限制。

不要分析或实施用户未确认的优化建议，不要修改已完成项目中的历史文件。
