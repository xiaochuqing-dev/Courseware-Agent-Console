# 阶段二实施报告

## 1. 阶段二目标

在不推翻阶段一架构的前提下，完成“客户反馈导入 → 指定反馈轮次 → 生成反馈修改任务 → 多轮产品迭代 → 完整产品验收 Prompt → 用户手动归档 → 已完成项目查看”的真实业务闭环。GUI 仍只执行确定性文件规则，不理解反馈、不调用模型、不自动判断客户满意。

## 2. 新增功能

- 支持 Ctrl+V 粘贴图片、文字、资源管理器已复制文件，以及拖入单个或多个文件。
- 反馈先进入待保存区，显示文件名、类型、文字预览或图片缩略图，可逐项移除。
- 支持追加最新轮次或由用户创建下一轮，按钮文案随磁盘中的真实轮次变化。
- 当前任务支持“首次制作”和“反馈修改”，反馈修改默认选择最新有效轮次。
- 增加当前任务预览、最新产品显示、手动刷新与窗口重新激活刷新。
- 更多操作中增加完整产品验收 Prompt 和打开项目记录。
- 增加用户确认归档、归档冲突保护和已完成项目查看页。

## 3. 未实现功能

按阶段边界未实现大模型 API、智能调度、反馈内容分析、客户满意度判断、数据库、云同步、多人协作、GitHub 推送、CI/CD、安装包和工作流优化模块。

## 4. 对阶段一架构的扩展

保留 MainWindow、QStackedWidget、Card、Toast、ProjectService、TaskService 和 QSettings 结构。新增 FeedbackService、ArchiveService、PromptService、反馈拖放控件、Prompt 对话框和 CompletedProjectsPage。UI 只收集用户选择，文件规则和路径判断集中在 Service。

## 5. 客户反馈导入机制

剪贴板图片转换为 PNG 内存项，文字以 UTF-8 保存为补充说明文件，外部文件在保存前只保留来源路径。用户确认轮次后才逐项原子落盘；单项失败不会撤销已成功文件，失败项继续留在待保存区。文件夹会被拒绝，不递归导入。

## 6. 反馈轮次规则

只识别“第N轮”标准目录并按数字排序，忽略 temp、截图等异常目录。无历史时保存为第1轮；有历史时由用户选择追加最新轮次或创建下一轮。保存时不覆盖同名文件，自动使用“文件 (2).扩展名”等名称。

## 7. 当前任务生成规则

首次制作生成初始版本任务。反馈修改要求指定一个真实存在的反馈轮次，特殊要求为空时写“无”。当前任务保持短索引，只列出本轮反馈、最新有效 HTML、项目记录、原始需求及公共工具；复制内容统一为“执行项目N当前任务。”。

## 8. 项目记录规则

GUI 只保证《项目记录.md》存在，并在任务与根规则中要求 Agent 客观追加首次制作或第 N 轮修改记录。规则明确禁止删除历史，也禁止写“客户满意”等无法由 Agent 判断的状态。

## 9. 产品验收 Prompt 机制

验收入口位于“更多操作”，不占用主流程阶段。Prompt 从资源模板生成，机械注入项目名和最新标准版本，要求读取项目记录、最新 HTML、原始需求及公共工具，并只在核对闭环时针对性查看历史反馈。验收结论不会改变项目状态。

## 10. 归档机制

最新产品按最大 N 的“第N轮修改.html”识别，无修改版时使用“初始版本.html”，不参考修改时间。无有效 HTML 时禁止归档。用户确认后把整个项目移动到同级“已完成项目/项目组名/项目N”；目标同名目录存在时拒绝覆盖或合并。

## 11. 已完成项目查看

已完成项目页可按归档项目组切换，按项目数字顺序展示项目，并支持打开项目文件夹、产品迭代目录和项目记录。归档后进行中列表立即刷新，不重编号剩余项目。

## 12. 测试结果

- Python 全量编译通过。
- 15 项 pytest 聚焦测试通过。
- 应用无交互启动检查通过。
- validate-tool.js 对内置 template.html 验证通过。
- 1280×840 和最小窗口 980×680 均完成真实 Qt 渲染检查，无控件重叠或关键文字截断。

## 13. 真实两轮反馈模拟结果

自动化模拟创建含项目3的项目组，写入初始版本，导入两张有效 PNG 和一段文字为第1轮，生成第1轮反馈任务并写入第1轮修改 HTML；随后导入第2轮图片和文字，生成第2轮任务并写入第2轮修改 HTML。系统正确识别第2轮修改为最新产品，成功生成验收 Prompt，并经同一确认归档入口把项目3移动到“已完成项目/九年级示例项目组/项目3”。已完成项目页可继续查看该项目。

## 14. 已知限制

- 待保存反馈只存在于当前运行内存，关闭应用前需要完成保存。
- 当前不预览 PDF、DOCX 等文件内容，只显示为普通文件，业务理解交给外部 Agent。
- 文件系统自动刷新采用手动刷新、页面切换和窗口重新激活，没有引入 QFileSystemWatcher。
- 现有阶段一项目组的自定义规则不会被自动覆盖；用户可在规则编辑器中二次确认恢复为阶段二最新模板。

## 15. 下一阶段建议

下一阶段可独立设计工作流复盘与经验沉淀，但仍应保持与生产任务、客户满意判断和项目归档状态解耦。安装包与应用更新也应作为单独交付工作处理。

## 16. 本地 Git commit

提交信息：feat: complete feedback iteration and project archive workflow

仓库只使用本地 Git，不配置远程，不推送 GitHub。
